feat: 完成 DataAgent Pro 多智能体平台（5 个阶段，83 个文件）

阶段 0：基础设施
- pyproject.toml（uv 依赖管理，Python 3.11+）
- docker-compose.yml（Redis+Milvus+MySQL+PostgreSQL）
- FastAPI + Vue 3 骨架

阶段 1：核心链路
- Schema Agent（动态加载表结构）
- SQL Coder Agent（Text-to-SQL + 自纠错重试）
- 聊天 UI（SQL 代码块 + 表格展示）

阶段 2：LangGraph 工作流
- 10 个 Agent 节点（Orchestrator → Schema → SQL → Security → Execute → Analyst → Reporter → Finish + RAG）
- Human-in-the-loop 审批机制（Redis 快照持久化）
- SSE 流式输出

阶段 3：RAG + Analyst + Reporter
- RAG Agent（Milvus 向量检索）
- Analyst Agent（同比/环比/极值分析）
- Reporter Agent（ECharts 图表配置）

阶段 4：多方言 + 审批 UI
- 4 种 SQL 方言（MySQL/PostgreSQL/SQL Server/Oracle）
- 前端审批管理页面
- CSV 流式导出
- Skill 集成预留

阶段 5：文档 + 部署 + 验收
- 13 个 VitePress 文档
- Dockerfile × 2 + nginx.conf
- 14 个测试（全部通过）

已知问题（3 个待修复）：
1. schema/sql_coder 节点缺少错误短路条件边
2. GraphInterrupt 用字符串匹配捕获
3. execute_node 丢失 SQL 自纠错重试能力