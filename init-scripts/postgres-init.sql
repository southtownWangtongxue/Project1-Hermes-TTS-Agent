-- PostgreSQL init script
-- The database business_db is auto-created via POSTGRES_DB env var.
-- This script runs after DB creation, use it to create tables etc.

-- Example table for testing (uncomment to use)
-- CREATE TABLE IF NOT EXISTS test_data (
--   id         BIGSERIAL PRIMARY KEY,
--   name       VARCHAR(255) NOT NULL,
--   created_at TIMESTAMP DEFAULT NOW()
-- );
